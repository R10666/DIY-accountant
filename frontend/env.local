# Personal override — NOT committed (see .gitignore). This keeps your
# local dev environment talking to the real FastAPI backend, exactly as
# it worked before localDB.js existed. Delete this file (or change the
# value) if you ever want this machine to use browser-only storage too.
VITE_STORAGE_MODE=server
